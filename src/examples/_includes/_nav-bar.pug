mixin d-nav-bar
    .comp
        .comp__descr
            p Настройки меню в файле <strong>src/style/_tools/_variable.scss</strong>
            <pre><code class="language-scss">
                .
                    $nav-background-color: #333;
                    $nav-color: #ccc;
                    $nav-item-hover-background-color: transparent;
                    $nav-item-hover-color: $accent-color;
                    $nav-padding: 0;
                    $nav-item-padding: 1.8em;
                    $nav-height: 100px;
                    $nav-item-height: 100px;
                    $nav-item-active-background-color: transparent;
                    $nav-item-active-color: $accent-color;
            </code></pre>
            p Вызов меню (пока так)
             <pre><code class="language-pug">
                .
                    +nav-bar()
             </code></pre>
        +nav-bar()